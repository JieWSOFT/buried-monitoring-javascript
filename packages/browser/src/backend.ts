import { BaseBackend } from "@bm/core";
import { Options } from "@bm/types";

export interface BrowserOptions extends Options {

}

/**
 * The BM Browser SDK Backend.
 * @hidden
 */
 export class BrowserBackend extends BaseBackend<BrowserOptions>{
     
 }